/**
 * Re-export do contexto de autenticação.
 * O contexto é definido em AuthContextRef.js; este arquivo existe para
 * compatibilidade com resolução case-insensitive (Windows) e cache do Vite.
 */
export { AuthContext } from './AuthContextRef'
